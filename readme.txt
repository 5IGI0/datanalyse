== datanalyse ==

Export CSV to database for easier dataset analysis.

== TODO ==
[X] detect column types while generating sql (instead of detecting it before)
[X] detect charset (mainly ASCII vs UTF-8)
[X] detect enum
[ ] allow auto reanalyze (./datanalyse update <directory with a subdirectory for each dataset>)
[ ] phonetical search